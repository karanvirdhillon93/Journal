public int[] copyEndy(int[] nums, int count) {
  int result[]= new int[count];
  int pos=0;
  
  for(int i=0;i<nums.length;i++){
    int check=nums[i];
    
    if((check<=100)&&(check>=90)) {
       result[pos]=check;
       pos++;
    }

    else if((check<=10) && (check>=0)){
        result[pos]=check;
         pos++;
      }
      
    if(pos>=count) break;
    
  }
  return result;
}
