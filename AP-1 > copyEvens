public int[] copyEvens(int[] nums, int count) {
  int result[]= new int[count];
  int pos=0;
  for(int i=0;i<nums.length;i++){
    if(nums[i]%2==0){
      result[pos]=nums[i];
      pos++;
    }
    if(pos>=count) break;
  }
  return result;
  
}
