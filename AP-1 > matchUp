public int matchUp(String[] a, String[] b) {
  int result=0;
  for(int i=0;i<a.length;i++){
    if(a[i]==""||b[i]==""){
      continue;
    }
    else{
      char aLetter=a[i].charAt(0);
      char bLetter=b[i].charAt(0);
      if(aLetter==bLetter){
        result++;
      }
    }
    
  }
  return result;
  
}
